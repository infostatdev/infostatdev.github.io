---
title: "infostatdev.github.io"
format:
  html:
   code-tools: true
   code-fod: true
---

## Quarto

```{r}
renv::init()
```


```{r}
renv::install("dplyr")
renv::install("ggplot2")
```


```{r}
#renv::update()
#renv::snapshot()
```

## Installation des packages
```{r}
#install.packages("dplyr")
#install.packages("ggplot2")
```

### Data analysis
```{r}
data(mtcars)
```


```{r}
df <- mtcars
head(df, 10)
str(df)
```

## Visualisation des données 

```{r}
library("ggplot2")
ggplot(df)+geom_histogram(aes(x=df$disp))
```
